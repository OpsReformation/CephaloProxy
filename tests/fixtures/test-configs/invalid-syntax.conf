# Invalid Squid configuration for testing error handling
# This config has intentional syntax errors

# Missing value - invalid
http_port

# Unknown directive - invalid
this_is_not_a_real_directive foo bar

# Incomplete cache_dir - invalid
cache_dir ufs

# Access rule without ACL definition - invalid
http_access allow nonexistent_acl

# This should cause squid -k parse to fail
